{
  "build": {
    "buildCommand": "pnpm install && cd backend && python3 -m pip install -r requirements.txt && cd .. && pnpm build"
  },
  "deploy": {
    "startCommand": "bash start.sh",
    "healthcheckPath": "/api/health",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10,
    "timeout": 120000
  }
}
