# Nixpacks configuration
# Add Python alongside Node.js for the FastAPI backend

[phases.setup]
nixPkgs = ["python311", "python311Packages.pip"]

[phases.install]
cmds = [
  "pnpm install",
  "cd backend && python3 -m pip install -r requirements.txt && cd ..",
  "pnpm build"
]
