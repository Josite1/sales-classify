# Nixpacks configuration
# Node.js is auto-detected from package.json. Python 3.11 added manually.

[phases.setup]
nixPkgs = ["python311", "python311Packages.pip"]

[phases.build]
cmds = ["cd backend && pip install -r requirements.txt"]

[start]
cmd = "bash start.sh"
